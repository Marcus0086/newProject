import React, { Component } from 'react';

class Arrays extends Component {
    constructor(props) {
        super(props);
        this.state = {
            empty:"",
        };
    }

    render() {
        return (
            <React.Fragment>
                <p>Hello arrays page</p>
            </React.Fragment>
            );
    }
}

export default Arrays;