const mergeSortAlgo = [
    ["1. function merge(left, right) {<br />"],
    ["2. let resultArray = [],<br />"],
    ["3. leftIndex = 0,<br />"],
    ["4. rightIndex = 0;<br />"],
];

let mergeSortFile = mergeSortAlgo.join(" ");
console.log(mergeSortFile);
export default mergeSortFile;
